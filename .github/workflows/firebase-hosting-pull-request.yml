# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm install
        
      - name: Install web dependencies
        run: npm install
        working-directory: ./web
        
      - name: Build web project
        run: npm run build
        working-directory: ./web
        
      # Skip Firebase deployment for now to avoid secret issues
      # TODO: Add FIREBASE_SERVICE_ACCOUNT_FOODTRUCKFINDER_27EBA to GitHub secrets
      # - uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: ${{ secrets.GITHUB_TOKEN }}
      #     firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_FOODTRUCKFINDER_27EBA }}
      #     projectId: foodtruckfinder-27eba
